name: "glitchtip"
description: "Genesis Glitchtip element"
schema_version: 1
version: "0.0.1-dev+20260121164522.4197b2e4"
api_version: "v1"

requirements:
  core:
    from_version: "0.0.0"

resources:
  $core.compute.nodes:
    genesis_glitchtip:
      name: "glitchtip"
      hostname: "glitchtip"
      project_id: "12345678-c625-4fee-81d5-f691897b8142"
      disk_spec:
        kind: root_disk
        size: 20
        image: http://10.20.0.1:8080/genesis-elements/{{ name }}/{{ version }}/images/genesis-glitchtip.raw.gz
      cores: 4
      ram: 8000

  $core.config.configs:
    genesis_glitchtip_config:
      project_id: "12345678-c625-4fee-81d5-f691897b8142"
      path: /etc/default/glitchtip.env
      target:
        kind: node
        node: $core.compute.nodes.$genesis_glitchtip:uuid
      body:
        kind: text
        content: |
          f"
          DATABASE_URL=postgres://postgres:postgres@postgres:5432/postgres
          SECRET_KEY=change_me_to_something_random # best to run openssl rand -hex 32
          PORT=80 # If changing, change the web service port too
          EMAIL_URL=consolemail:// # Example smtp://email:password@smtp_url:port https://glitchtip.com/documentation/install#configuration
          GLITCHTIP_DOMAIN=http://$core.compute.nodes.$genesis_glitchtip:default_network:ipv4
          CSRF_TRUSTED_ORIGINS=http://$core.compute.nodes.$genesis_glitchtip:default_network:ipv4
          DEFAULT_FROM_EMAIL=email@glitchtip.user.com
          CELERY_WORKER_AUTOSCALE="1,3" # Scale between 1 and 3 to prevent excessive memory usage. Change it or remove to set it to the number of cpu cores.
          "


